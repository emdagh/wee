PROJECT(wee_engine)

FILE(GLOB SOURCES
    "*.cpp"
    "*.c"
)

ADD_LIBRARY(
    ${PROJECT_NAME}
    ${LIBRARY_TYPE}
    ${SOURCES}
)

TARGET_LINK_LIBRARIES(${PROJECT_NAME})


INSTALL(TARGETS ${PROJECT_NAME} DESTINATION .)
